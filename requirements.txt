# ============================================================
# Formula 1 Race Prediction Platform — Python dependencies
# Python >= 3.11 recommended
# Install: pip install -r requirements.txt
# ============================================================

# --- Data ingestion -----------------------------------------
requests==2.31.0            # HTTP client for Ergast & OpenF1 REST APIs
fastf1==3.3.9               # FastF1 session interface + disk caching

# --- Data handling ------------------------------------------
pandas==2.2.2               # DataFrames, Parquet I/O
pyarrow==16.1.0             # Parquet engine (required by pandas)
numpy==1.26.4               # Numerical operations

# --- Database -----------------------------------------------
psycopg2-binary==2.9.9      # PostgreSQL driver (binary = no build deps)
sqlalchemy==2.0.30          # ORM / connection pooling used in src/utils/db.py

# --- Feature engineering & ML -------------------------------
scikit-learn==1.5.0         # Logistic Regression, Random Forest, preprocessing
xgboost==2.0.3              # XGBoost classifier
shap==0.45.1                # Feature importance / SHAP values (optional in app)
imbalanced-learn==0.12.3    # SMOTE / class-imbalance handling

# --- Model persistence --------------------------------------
joblib==1.4.2               # Model serialisation (.pkl artifacts)

# --- Evaluation & metrics -----------------------------------
matplotlib==3.9.0           # Confusion matrix plots, EDA charts
seaborn==0.13.2             # Statistical visualisation in notebooks

# --- Notebooks ----------------------------------------------
jupyter==1.0.0              # Notebook server
ipykernel==6.29.4           # Kernel for VS Code / JupyterLab

# --- App deployment -----------------------------------------
streamlit==1.35.0           # Interactive prediction UI

# --- Config & utilities -------------------------------------
pyyaml==6.0.1               # config/settings.yaml loader
python-dotenv==1.0.1        # .env support for DB credentials
tqdm==4.66.4                # Progress bars in ingestion loops

# --- Testing ------------------------------------------------
pytest==8.2.1               # Test runner for tests/
pytest-cov==5.0.0           # Coverage reporting
